// Basic function to fade in feature image
require('./includes/images_loaded');
// Pass it a classname and it will turn the image in to a lightbox
const lightbox = require('./includes/lightbox');

lightbox('[data-lightbox]');
